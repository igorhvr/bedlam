
(module info (lib "infotab.ss" "setup")

  (define name "fmt")
  (define blurb
    '("Extensible Combinator-based Text Formatting"))
  (define categories '(io))
  (define html-docs '("fmt.html"))
  (define homepage "http://synthcode.com/fmt/")
  (define primary-file "fmt.ss")
  (define version "0.6")

  )

